# Perago Review 规则

## Review 重点

- 修改 `src/perago/workspace/` 或 `src/perago/lakefs_runtime.py` 时，追踪对象列表、同步计划、staging、publish 与 cleanup 的完整路径；同名文件可以存在于不同子目录和不同 LakeFS repository。
- 修改 `src/perago/taskdef.py` 或 task contract 测试时，核对 `perago check`、`perago extract`、`perago start` 与直接调用 `build_taskdef()` 的一致性。
- 修改 process/thread runner 时，同时核对两种 execution mode，不把其中一种的测试结果当作另一种的证据。

## 性能边界

- 一个 workspace prefix 可能包含 100,000 个本地或远程对象；同步计划应保持近似线性扫描，不得为每个对象重新扫描完整集合或增加远程请求。
- `-j` 由部署配置，不能假定为小常数；process/thread 路径中的工作量需要按 worker 数和 task 数检查。

## Known issues

- `Qiyin-Tech/perago` 的 GitHub Issue、历史 PR 正文、Review thread 和维护者评论属于 known issue 来源；closed/resolved 只表示状态线索，具体修复或决定以记录内容为准。
- 修改 Conductor polling、runner 启动或 jitter 时，定向查询相关 GitHub Issue 和历史 PR，不预读无关记录。
- 修改 task model、schema 清理或 RootModel 处理时，定向查询相关 GitHub Issue 和引入该约束的历史 PR。
- 修改 workspace repository identity、staging cleanup 或并发 runtime 状态时，定向查询相关历史 PR 和 Review thread。
