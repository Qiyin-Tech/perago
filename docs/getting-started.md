# Getting Started

本页是 Perago 文档站点的本地构建占位入口。完整的新任务作者路径会在后续迭代中从 `docs/mvp_examples.md` 和测试 fixture 拆分到 `task-authoring/`。

当前可用的稳定入口：

- `task-authoring/`：任务作者指南。
- `runtime/`：运行时、Conductor、LakeFS 和 worker supervisor。
- `api/`：从 `perago.__all__` 生成的公开 API reference。
